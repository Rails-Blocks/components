# Rails Blocks Components

This repository contains the permissive open-source Rails Blocks components generated from the Rails Blocks app.

The canonical machine-readable entrypoint is `registry.json`. CLI and MCP clients should prefer the registry and immutable package artifacts under `packages/0.1.0`.
