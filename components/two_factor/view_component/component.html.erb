<div class="<%= wrapper_classes %>">
  <% if @label.present? %>
    <label class="<%= label_classes %>">
      <%= @label %>
      <% if @required %><span class="text-red-500">*</span><% end %>
    </label>
  <% end %>

  <div data-controller="two-factor"
       data-two-factor-auto-submit-value="<%= @auto_submit %>"
       data-two-factor-autofocus-value="<%= @autofocus %>"
       data-two-factor-numeric-only-value="<%= @numeric_only %>">

    <div class="<%= inputs_wrapper_classes %>">
      <% @length.times do |i| %>
        <input
          data-two-factor-target="digit"
          data-action="input->two-factor#handleInput keydown->two-factor#handleKeydown paste->two-factor#handlePaste"
          type="text"
          inputmode="<%= input_mode %>"
          id="<%= input_id(i) %>"
          name="<%= input_name(i) %>"
          maxlength="1"
          autocomplete="<%= autocomplete_value(i) %>"
          <% if @required %>required<% end %>
          <% if @disabled %>disabled<% end %>
          class="<%= input_classes %>">

        <% if show_separator_after?(i) %>
          <span class="<%= separator_classes %>">-</span>
        <% end %>
      <% end %>
    </div>
  </div>

  <% if @error.present? %>
    <p class="<%= error_classes %>"><%= @error %></p>
  <% elsif @hint.present? %>
    <p class="<%= hint_classes %>"><%= @hint %></p>
  <% end %>
</div>
